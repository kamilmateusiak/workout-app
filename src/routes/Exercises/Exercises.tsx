/** @jsx jsx */
import React from 'react';
import { jsx } from '@emotion/core'

interface IProps {};

export const Exercises: React.FC = (props: IProps) => {
  return (
    <div>
      <h1>Exercises</h1>
    </div>
  );
}
